<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Product</title>
</head>

<body>
    <h1>Edit New Product</h1>
    <form method='POST' action="/products/<%= products._id %>/edit/?_method=PATCH">
        <label for="name">Product Name:</label>
        <input type="text" name="name" id="name" value="<%= products.name %> " required>
        <label for="number">Enter price: $</label>
        <input type="number" name="price" id="price" min="0" max="100" step="0.01"
            placeholder=" <%= products.price %>  ">
        <label for="category">Select Category:</label>
        <select name="category" id="category">
            <% for(cat of category){ %>
                <option value="<%= cat %>" <%=products.category===cat ? 'selected' : '' %> ><%= cat %>
                </option>
                <% } %>


        </select>
        <button>Submit</button>
    </form>
    <a href="/products">Home</a>
</body>

</html>